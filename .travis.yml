language: node_js

node_js: ["0.12", "4", "5", "6"]

addons:
    apt:
        sources:
            - ubuntu-toolchain-r-test
        packages:
            - g++-4.8
            - libqrencode-dev
            - libpng12-dev

env:
    - CXX=g++-4.8
